<%- include("header") %>

<body>

  <%- include("leftbar") %>


  <%- include("navbar") %>


<div class="br-mainpanel">
    <div class="br-pagetitle">
        <div class="container-fluid p-5">
            <h2 class="text-center">Update Categories</h2>
            <form method="post" action="/update_category/<%= category._id %>" id="update_category" enctype="multipart/form-data">

                <div class="form-group">
                    <label for="title">Title</label>
                    <input type="hidden" name="id" value="<%= category._id %>">
                    <input name="title" class="form-control" placeholder="Enter title" value="<%= category.title %>" required>
                </div>
                <div class="form-group">
                    <label for="image" >Thumbnail</label>
                    <input name="image" class="form-control" type="file" value="<%= category.image %>" >
                    <img src="/../../static/uploads/<%= category.image %>" width="100" class="img-thumbnail" my-1>
                </div>
                <input type="hidden" name="old_image" value="<%= category.image %>">
                <div class="form-group py-2">
                    <label for="status" class="text-dark">Status: </label>
                    <div class="radio form-check form-check-inline">
                        <input type="radio" id="radio4" name="status" value="Active"<%= category.status=='Active'?'checked':'' %>>
                        <label for="radio4" >Active</label>
                    </div>
                    <div class="radio form-check form-check-inline">
                        <input type="radio" id="radio5" name="status" value="Inactive"<%= category.status=='Inactive'?'checked':'' %>>
                        <label for="radio5">Inactive</label>
                    </div>
                </div>
                <div class="form-group py-2">
                    <button type="submit" class="btn btn-primary">Save</button>
                </div>   
            </form>   
        </div>
    </div>
</div>


<%- include("footer") %>
